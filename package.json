{
  "name": "orca-project",
  "version": "1.0.0",
  "description": "ORCA - Ocean Reasoning & Collaborative Agents",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "frontend": "npm run dev --prefix frontend",
    "frontend:build": "npm run build --prefix frontend",
    "frontend:preview": "npm run preview --prefix frontend"
  }
}

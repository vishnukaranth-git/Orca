{
  "name": "orca-frontend",
  "version": "1.0.0",
  "description": "ORCA Ocean Reasoning & Collaborative Agents Frontend UI",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000",
    "build": "vite build && node copy-assets.js",
    "preview": "vite preview --port 3000",
    "start": "vite --port 3000 --host"
  },
  "dependencies": {
    "vite": "^6.0.0"
  }
}
